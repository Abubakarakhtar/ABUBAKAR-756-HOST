const $=s=>document.querySelector(s);
document.querySelectorAll(".tab").forEach(b=>b.onclick=()=>{document.querySelectorAll(".tab").forEach(x=>x.classList.remove("active"));b.classList.add("active");document.querySelectorAll(".tabbody").forEach(x=>x.classList.add("hidden"));$("#"+b.dataset.tab).classList.remove("hidden")});
$("#theme").onclick=()=>{document.body.classList.toggle("light");document.body.classList.contains("light")?$("#theme").textContent="🌙":$("#theme").textContent="☀️"};
$("#deployPaste").onclick=()=>{let n=($("#project").value||"my-site").trim().replace(/[^a-zA-Z0-9-_]/g,"-").toLowerCase();let h=$("#html").value.trim();if(!h){alert("HTML code paste karo.");return}let blob=new Blob([h],{type:"text/html"}),url=URL.createObjectURL(blob);$("#result").classList.remove("hidden");$("#result").innerHTML=`<b>Deployment package ready ✅</b><p>Project: ${n}</p><button id="download">Download index.html</button>`;$("#download").onclick=()=>{let a=document.createElement("a");a.href=url;a.download=n+"-index.html";a.click()}};
$("#deployZip").onclick=()=>{let f=$("#zip").files[0];if(!f){alert("ZIP select karo.");return}$("#result").classList.remove("hidden");$("#result").innerHTML="<b>ZIP selected ✅</b><p>"+f.name+" — ab is ZIP ko Cloudflare Pages Direct Upload mein publish kar sakte ho.</p>"};
if("serviceWorker" in navigator) navigator.serviceWorker.register("sw.js").catch(()=>{});
