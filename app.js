const $=id=>document.getElementById(id);$('deploy').onclick=async()=>{const api=$('api').value.trim().replace(/\/$/,''),project=$('project').value.trim(),html=$('html').value;if(!api||!project||!html){$('out').textContent='Enter Worker URL, project name and HTML.';return}$('out').textContent='Deploying…';try{const r=await fetch(api+'/deploy',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({project,html})});const text=await r.text();$('out').textContent=text;try{const j=JSON.parse(text);if(j.url) $('out').textContent+='\n\nLIVE URL: '+j.url}catch{}}catch(e){$('out').textContent='Request failed: '+e.message}};
