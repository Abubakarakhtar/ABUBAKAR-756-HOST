const $=id=>document.getElementById(id);$('send').onclick=async()=>{const api=$('api').value.trim().replace(/\/$/,''),name=$('name').value.trim(),html=$('html').value;if(!api||!name||!html){$('out').textContent='API URL, project name aur HTML required hain.';return}$('out').textContent='Sending…';try{const r=await fetch(api+'/deploy',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({project:name,html})});$('out').textContent=JSON.stringify(await r.json(),null,2)}catch(e){$('out').textContent='Request failed: '+e.message+'\nCheck Worker URL and CORS.'}};
